<!DOCTYPE html>
<html>
  <head>
    <title>RailsRuby3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"%>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
      <div class ="container main">
        <div class= "row">
          <div class ="col-md-12">
            <%flash.each do |name, message| %>
              <div class="alert <%= bootstrap_class_for(name) %>"
                <button class="close" data-dismis="alert">&times;</button>
                <%= message%>
              </div>
              <% end %>
          </div>
      </div>
      <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 border-bottom shadow-sm">
      <h5 class="my-0 mr-md-auto font-weight-bold">Website</h5>
      <nav class="d-inline-flex mt-5 mt-md-0 ms-md-auto">
        <span class="me-4 py-text-dark"><%= link_to "Главная страница", home_path %></span>
        <span class="me-4 py-text-dark"><%= link_to "Про проект", about_path %></span>
        <span class="me-4 py-text-dark"><%= link_to "Магазин", shop_path %></span>
      </nav>
      <span class ="py-2 text-dark" ><%= link_to "Добавление статьи", new_post_path %></span>
      <ul class = "navbar-nav mr-auto">
       <% if user_signed_in? %>
         <li class="nav-item">
           <%= link_to "Выход", destroy_user_session_path, method: :delete, class: "nav-link" %>
       </li>
         <%else %>
       <li class="nav-item">
         <%= link_to "Вход", new_user_session_path, class: "nav-link" %>
       </li>
       <li class="nav-item">
        <%= link_to "Регистрация", new_user_registration_path, class: "nav-link" %>
       </li>
           <% end %>
     </ul>
    </div>
    <div class="container">
    <%= yield %>
    </div>
  </body>
      
</html>
